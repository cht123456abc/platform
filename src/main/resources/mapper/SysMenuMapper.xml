<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.edu.scu.platform.mapper.SysMenuMapper">

    <select id="queryListByParentId" resultType="cn.edu.scu.platform.entity.SysMenu">
		select `menu_id`,
			`parent_id`,
			`name`,
			`url`,
			`perms`,
			`type`,
			`icon`,
			`order_num`,
			`status`
		from sys_menu where parent_id = #{parentId} AND status = 0 order by order_num asc
	</select>

</mapper>
